// js/analytics.js
console.log('📈 analytics.js: Скрипт-оркестратор для аналитики загружен.');

document.addEventListener('DOMContentLoaded', () => {
    // Эта функция будет вызвана, когда весь HTML загрузится.
    // Скрипты для графиков (barChart.js, lineChart.js и т.д.) 
    // уже подключены в analytics.html и начнут работать самостоятельно,
    // так как у них у каждого есть свой обработчик DOMContentLoaded.
    
    console.log('Страница аналитики полностью загружена. Графики должны начать инициализацию.');

    // Здесь можно будет добавить дополнительную логику, если потребуется,
    // например, для взаимодействия между разными графиками.
});